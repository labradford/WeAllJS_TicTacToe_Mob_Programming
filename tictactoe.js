console.log("The js file has loaded!");

document.addEventListener("DOMContentLoaded", ready);

var gameBoard = [0,0,0,
                 0,0,0,
                 0,0,0];

console.log(gameBoard);

function ready() {
  console.log("Let's build tictactoe!")
}
